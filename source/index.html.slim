- past_events = data.webmondays.map { |past| past if parse_date(past.date) < parse_date(today_date)}.compact
- upcoming_events = data.webmondays.map { |later| later if parse_date(later.date) >= parse_date(today_date)}.compact
- next_event = upcoming_events.pop
.b-next-event
  .row
    .b-next-event--box.medium-12.large-9.columns
      = partial "shared/content/_event_box.slim",
      locals: {locale: 'de', next_event: next_event, upcoming_events: upcoming_events}
.row
  .small-12.medium-11.medium-offset-1.columns
    = partial "shared/index/_intro.slim", locals: {next_event: next_event}

.row
  .text-center.small-12.medium-11
    .b-submit-talk
      h3.b-submit-talk--title
        | Du hast was erzählen?
      p.b-submit-talk--text
        |Web Montag ist ein informelles, nicht-kommerzielles, dezentral organisiertes Treffen, das zum Ziel hat, all diejenigen miteinander zu verbinden, die die Zukunft des Internet gestalten. Inspiriert von der Kultur Silicon Valleys startete der Web Montag gegen Ende 2005 in Köln als Versuch, ein bisschen “kalifornischen Sonnenschein” nach Deutschland zu bringen.
      a.a-button[href="#"]
        | Talk einreichen
